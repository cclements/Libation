## Problem and resulting behavior

<!-- State the trigger and resulting behavior. Link the relevant issue/packet.
For UI changes, name affected surfaces and target/recipe; explain intentional
design differences. Omit UI-only details for a change without a UI effect. -->

## Validation and remaining limits

<!-- Report checks actually run and their results. For user-visible changes,
include relevant before/after images, profile/window-size coverage and changed
keyboard/error/cancel behavior. Distinguish rendered, interaction, accessibility
and installed-platform evidence. Link the updated coverage/packet receipt.
Documentation-only changes use document/link/diff inspection, not app tests.
For audio/native/package changes, identify the input/profile and output behavior,
relevant boundary/failure checks and exact dependency/native artifact identity.
Separate source or package construction from real decode/provider/installed proof;
link the owning finding/packet receipt and retain any unsupported/untested scope.
Keep this section proportionate; do not replace missing proof with a checklist pass. -->
